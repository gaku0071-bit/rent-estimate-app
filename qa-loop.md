# Mac・Windows共通 QA ループ

## 目的

この文書は、機能追加ではなく「実務で使える状態」を確認するための共通テスト手順です。
MacとWindowsで同じテスト番号を実行し、どちらか一方でも失敗した項目は合格にしません。

最終合格は100点満点です。点数が100点でも、P0またはP1の不具合が1件でも残っていれば不合格とします。

## 採点

| 分野 | 配点 | 合格条件 |
|---|---:|---|
| 起動・リセット | 10 | 初回起動、再起動、リセット、途中データ復元ができる |
| 画面・ボタン操作 | 15 | ボタンが正しい状態で表示され、押下後の状態が分かる |
| 条件検索 | 15 | 路線・駅、間取、賃料、徒歩、設備、駐車場が反映される |
| リアプロ連携 | 20 | 件数、ページ、詳細、追加取得、進捗表示が正しい |
| いい生活照合 | 15 | 全件照合、募集中、未登録、確認中の判定が正しい |
| 候補判定 | 10 | 掲載不可、申込、商談・審査、重複、差替が正しい |
| 候補リスト・リンク | 5 | 完了後だけ表示され、号室リンクが正しい |
| CSV出力 | 5 | 画面と件数が一致し、文字化け・欠落がない |
| 大量件数・中断耐性 | 3 | 500件以上、追加取得、中断、再開で壊れない |
| インストール・更新 | 2 | Mac・Windowsの導入、更新通知、更新後起動ができる |
| **合計** | **100** | **Mac・Windowsの両方で合格** |

### 実Chrome操作の内訳

実Chromeを使う項目は、主に「リアプロ連携」20点として次のように採点します。

| 項目 | 点数 | 満点条件 |
|---|---:|---|
| Chrome拡張接続・対象タブ | 3 | リアプロ一覧といい生活物件一覧を正しく認識する |
| 条件反映 | 4 | 路線・駅、間取、賃料、徒歩、設備が実際のリアプロ画面へ反映される |
| 件数の扱い | 2 | 前回件数を使わず、今回の検索結果件数を記録する |
| リアプロ少数取得 | 3 | 指定ページだけ取得し、実Chromeの結果と件数が一致する |
| 取得中の進捗 | 2 | ページ、号室、詳細確認、候補見込みが増える |
| 状態表示・点滅 | 2 | 実行中だけ点滅し、待機中は「追加取得待ち」などになる |
| 追加取得・バッチ保持 | 2 | 既取得ページを省き、1回目の候補を残して2回目を追加する |
| いい生活照合・完了通知 | 1 | 全件照合後に通知が出て、確定候補を開ける |
| **合計** | **20** | **実Chromeで全項目合格** |

次のどれかが起きた場合、この20点は満点にしません。

- Chromeタブ確認では対象タブが見えるのに、条件反映や取得が別経路で失敗する
- リアプロのトップ画面や建物一覧を検索結果一覧として扱う
- 前回の件数、前回の候補、前回の検索条件を今回の結果として表示する
- 取得中なのにページ数・件数・最終更新時刻が一定時間変わらない
- 追加取得待ちなのに「リアプロ確認中」と点滅し続ける
- 条件反映が失敗したのに取得処理を開始する
- いい生活全件照合前に確定候補リストを表示する

## 重大度

- **P0**: 起動不可、データ消失、誤登録につながる誤判定
- **P1**: 主要ボタン停止、取得停止、誤った物件リンク、未確認データの候補混入
- **P2**: 進捗表示の誤り、分かりにくい状態表示、再試行しにくい操作
- **P3**: 文言、余白、軽微な見た目の問題

P0/P1は点数に関係なくリリース不可です。

## 共通テストケース

各ケースのMac結果とWindows結果を別々に記録します。

| ID | テスト | 期待結果 | Mac | Windows |
|---|---|---|---|---|
| C01 | 初回起動 | システム画面が開き、準備状態が表示される | □ | □ |
| C02 | 再起動 | 前回の実務データを壊さず起動する | □ | □ |
| C03 | リセット | 現在の検索・候補状態だけが初期化される | □ | □ |
| C04 | Chromeタブ確認 | リアプロ・いい生活の対象タブを検出する | □ | □ |
| C05 | 条件入力 | 保存した条件と画面表示が一致する | □ | □ |
| C06 | 路線・駅 | 路線選択後に駅候補が表示され、選択が反映される | □ | □ |
| C07 | 間取・賃料 | 1LDK、2LDK、3LDK以上、賃料帯が反映される | □ | □ |
| C08 | 設備・駐車場 | 設備と駐車場空きありが条件として反映される | □ | □ |
| C09 | 条件反映 | 「反映してリアプロ検索」で検索画面に反映される | □ | □ |
| C10 | 件数確認 | 件数が前回値と混ざらず、今回の検索結果として表示される | □ | □ |
| C11 | 少数取得 | 1〜2ページの取得が完了し、件数が増えていく | □ | □ |
| C12 | 取得進捗 | 取得ページ、取得号室、判定済み件数、状態が更新される | □ | □ |
| C13 | 取得中表示 | 「リアプロ確認中」などが点滅し、完了後に停止する | □ | □ |
| C14 | リアプロ追加取得 | 既取得ページを省き、追加ページだけ取得する | □ | □ |
| C15 | 追加取得中の候補 | 1回目の候補を保持し、2回目は別バッチとして管理する | □ | □ |
| C16 | いい生活全件照合 | 全件確認が完了するまで確定候補リストを出さない | □ | □ |
| C17 | 照合中表示 | 「いい生活確認中」と点滅し、完了後に停止する | □ | □ |
| C18 | 完了通知 | 照合完了時に通知が出て候補リストへ移動できる | □ | □ |
| C19 | 未登録判定 | いい生活にない部屋が新規登録候補になる | □ | □ |
| C20 | 募集中判定 | いい生活で同じ号室が募集中・掲載中なら候補外になる | □ | □ |
| C21 | 差替判定 | 既掲載号室がリアプロで募集対象外なら別号室が差替候補になる | □ | □ |
| C22 | 掲載不可 | WEB広告掲載不可が候補から除外される | □ | □ |
| C23 | 管理会社確認 | 管理会社確認が候補から消えず、確認対象と明示される | □ | □ |
| C24 | 申込・商談・審査 | 申込あり、商談中、審査中が候補から除外される | □ | □ |
| C25 | 同間取重複 | 同一建物・同間取は1部屋だけ候補になる | □ | □ |
| C26 | 未確認 | 詳細未確認が候補リスト・作業対象CSVに入らない | □ | □ |
| C27 | 号室リンク | クリックした号室のリアプロ詳細が開く | □ | □ |
| C28 | 候補表示 | いい生活確認完了後に候補リストが表示される | □ | □ |
| C29 | CSV件数 | 画面、候補CSV、作業対象CSVの件数が整合する | □ | □ |
| C30 | CSV品質 | 文字化け、列欠落、URL欠落、重複がない | □ | □ |
| C31 | 再検索 | 同じ条件の再検索で前回データが混ざらない | □ | □ |
| C32 | 中断・再開 | Chrome操作や通信が一時中断しても状態が分かり、再開できる | □ | □ |
| C33 | 大量件数 | 500件以上でページ漏れ・重複・画面停止がない | □ | □ |
| C34 | 更新通知 | 起動時に新しい更新があれば通知される | □ | □ |
| C35 | 更新後確認 | 更新後もデータ、拡張機能、ログイン状態の扱いが仕様どおり | □ | □ |

## 実Chromeを使う必須条件

自動テストの合格だけでは実務合格にしません。C04、C09〜C18、C27、C32、C34〜C35は、実際にログイン済みのChromeを操作して確認します。

実行前に、同じChromeで次の2画面を開きます。

- リアプロ: リスト検索結果一覧。トップ画面や建物一覧だけでは不可
- いい生活: `rent.e-bukken.app` の物件広告一覧・物件一覧。営業支援の画面は不可

実Chromeテストで確認すること:

- システムの「Chromeタブ確認」が2つの対象画面を認識する
- 条件反映後、リアプロ画面の検索条件が変わる
- リアプロで目視確認した今回の件数が、システムの今回の件数として記録される
- 取得中に実際のChromeタブを別作業で使っても、進捗表示が止まらない
- 取得ページ、号室、詳細確認、いい生活照合の進捗が増える
- いい生活全件確認が終わるまで確定候補を表示しない
- 候補の号室リンクが、実際のリアプロ物件詳細を開く

### 1回のChrome操作ループ

採点時は、次の順番を1回のループとして記録します。

1. システムをリセットする
2. 同じChromeでリアプロのリスト検索結果と、いい生活の物件広告一覧を開く
3. `Chromeタブ確認` を押し、2つの対象タブが準備OKになることを確認する
4. システム条件をリアプロへ反映する
5. リアプロ画面で反映された条件と今回の件数を目視確認する
6. 1〜2ページだけ取得する
7. 取得ページ、号室、詳細確認、候補見込み、最終更新が動くことを確認する
8. いい生活確認が完了するまで、状態表示と候補表示の扱いを確認する
9. 完了通知から候補リストを開く
10. 号室リンクを1件開き、リアプロ詳細が一致することを確認する
11. 次のページを追加取得する
12. 1回目の候補が残り、2回目の候補が追加されることを確認する
13. CSVの件数と画面の件数を確認する

このループを同じ条件で3回実行します。1回でも条件反映、進捗、候補確定、リンク、CSVのいずれかが失敗した場合、その項目は不合格です。

リアプロがトップ画面、建物一覧、ログイン画面の場合は、そのテストは合格にせず「準備未完了」と記録します。件数を前回の取得結果から補完して合格扱いにはしません。

## Mac専用テスト

- `.command` から起動できる。
- ローカルサーバーが8765番、使用中なら別ポートで起動する。
- ChromeのApple Events許可がない場合に原因が表示される。
- Mac用更新ZIPの展開・バックアップ・復元ができる。
- Mac側で `npm run verify:basic`、`npm run verify:sample`、`npm run verify:latest-exports` が通る。

## Windows専用テスト

- インストーラーから導入できる。
- デスクトップ・スタートメニューのショートカットから起動できる。
- Windows本体にNode.js/Pythonがなくても、同梱ランタイムで起動できる。
- Windows Defender / SmartScreenで止まった場合に原因を確認できる。
- 専用Chrome、拡張機能、Chrome接続ブリッジが起動する。
- `.bat`、PowerShell、Windowsパス、ユーザー名や日本語フォルダで動作する。
- Windows版インストーラー更新後も `data`、`exports`、`outputs` が保持される。
- アンインストール後の実務データの扱いが案内どおりになる。

## 1回のテストの進め方

1. バージョン、OS、Chrome、拡張機能の状態を記録する。
2. システムをリセットする。
3. C01〜C35を同じ検索条件で実行する。
4. 失敗したケースはスクリーンショット、時刻、操作、表示文言を残す。
5. P0/P1から修正する。
6. 修正後に失敗ケースを再実行する。
7. その後、関連ケースとC01〜C35全体を再実行する。
8. MacとWindowsの結果を比較し、両方合格した項目だけ合格にする。

## 実行記録テンプレート

```text
実行日:
バージョン:
OS:
Chrome:
拡張機能:
Chrome拡張バージョン:
Chrome対象タブ: リアプロ一覧 / いい生活物件一覧
検索条件:
リアプロ取得ページ:
リアプロ取得件数:
いい生活取得件数:
確定候補件数:
採点:
P0/P1:

失敗ケース:
- ID:
  実際の結果:
  期待結果:
  重大度:
  証拠:
  修正バージョン:

判定: 合格 / 不合格
```

## リリース判定

次のすべてを満たした場合だけ、Windows配布と実務利用へ進めます。

- Mac共通テスト35件が合格
- Windows共通テスト35件が合格
- Mac専用テストが合格
- Windows専用テストが合格
- 自動確認コマンドがすべて合格
- P0/P1が0件
- 画面候補、候補CSV、作業対象CSVの件数が一致
- 未確認データが確定候補に混入していない
- 追加取得・再検索・リセットを実行しても候補データが壊れない
- 実Chrome操作ループを同じ条件で3回連続成功
- Chrome拡張の対象タブ、条件反映、取得、進捗、追加取得が同じ経路で成功
